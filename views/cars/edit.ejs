<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
</head>

<body>
	<!-- update the header -->
	<h1>Edit <%= carDoc.name %>
	</h1>
	<form action="/cars/<%= carDoc._id %>?_method=PUT" method="POST">
		<label for="name">Name:</label>
		<input type="text" name="name" id="name" value="<%= carDoc.name %>"/>

		<label for="isReadyToDrive">Ready to Drive?</label>
		<input type="checkbox" name="isReadyToDrive" id="isReadyToDrive" 
		<% if (carDoc.isReadyToDrive) { %>
			checked
		<% } %>
		/>
		<!-- update the button text -->
		<button type="submit">Update Car</button>
	</form>
	<a href="/cars">Back to Cars</a>

	

</body>

</html>